# ==================== BACKEND CONFIGURATION ====================

# JWT Authentication
JWT_SECRET=change-me-in-production-use-long-random-string-min-32-chars
JWT_ALGORITHM=HS256

# Google Gemini API (REQUIRED)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_CHAT_MODEL=models/gemini-2.5-flash
GEMINI_EMBED_MODEL=models/text-embedding-004

# Firebase / Firestore (REQUIRED - use ONE of the following options)
# Option A: Path to service account file (for local dev)
FIREBASE_CREDENTIALS_PATH=./serviceAccount.json

# Option B: Inline JSON string (for Railway/Render deployment)
# Copy entire contents of serviceAccount.json and paste as single line
FIREBASE_CREDENTIALS_JSON=

# Option C: Base64 encoded credentials (alternative for deployment)
FIREBASE_CREDENTIALS_BASE64=

# OpenAI API (OPTIONAL - for embeddings/chat)
OPENAI_API_KEY=

# Application Settings
ENVIRONMENT=development
LOG_LEVEL=INFO

# Server Config (for production deployment)
PORT=8000
HOST=0.0.0.0

# CORS (comma-separated origins for production, use * for dev)
ALLOWED_ORIGINS=*

# ==================== FRONTEND CONFIGURATION ====================

# API URL (Vite environment variable - used at BUILD time)
# Local dev: http://127.0.0.1:8000
# Production: https://your-backend.up.railway.app
VITE_API_URL=http://127.0.0.1:8000
